import * as React from "react";
import * as styles from "../styles/index.less";

export const AuthPanel: React.FC = () => {
    return (
        <div className={styles.auth_panel_wrapper}>
            auth panel
        </div>
    );
}